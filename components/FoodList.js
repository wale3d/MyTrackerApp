import React, { Component } from "react";
import { View, Text } from "react-native";

const FoodList = (props) => {
  return (
    <View>
      <Text>
        This page will display all food eaten with the time we enter it.
      </Text>
    </View>
  );
};

export default FoodList;
